pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: bunx biome check --staged --no-errors-on-unmatched --diagnostic-level=error {staged_files}
    - name: typecheck
      run: bunx tsc --noEmit
